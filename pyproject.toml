[project]
name = "geometry-car"
version = "0.1.0"
requires-python = ">=3.13"
description = "Dagster pipeline that inventories the world of GTFS feeds and publishes the catalog"
dependencies = [
    "dagster>=1.9",
    "dagster-webserver>=1.9",
    "dagster-postgres>=0.25",
    "httpx>=0.27",
    "pydantic-settings>=2.6",
    "sqlalchemy>=2.0",
    "alembic>=1.14",
    "psycopg2-binary>=2.9",
    "railroad-club",
]

[tool.uv.sources]
railroad-club = { git = "https://git.kcfam.us/gtfs.zone/railroad-club.git" }

[dependency-groups]
dev = [
    "commitizen",
    "moto[s3]>=5.0",
    "pre-commit",
    "pytest>=8.0",
    "respx>=0.21",
    "ruff",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/geometry_car"]

[tool.ruff]
target-version = "py313"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "ANN", "TC", "RUF"]

[tool.ruff.lint.per-file-ignores]
# Tests take fixtures by name and return nothing worth annotating.
"tests/**" = ["ANN"]

[tool.commitizen]
name = "cz_conventional_commits"
tag_format = "v$version"
version_provider = "uv"
update_changelog_on_bump = true
major_version_zero = true

[tool.pytest.ini_options]
testpaths = ["tests"]
